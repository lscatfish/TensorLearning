# 鸢尾花分类问题的人工智能方法对比研究

> 人工智能课程大作业

## 摘要

本实验在 Iris 鸢尾花数据集上对比了 8 类 12 个分类方法的性能。为克服单次 70/30 划分的高方差，实验采用 10 次重复试验，并将全部试验的测试集预测拼接，统一计算 F1、ROC 和混淆矩阵。结果表明：MLP(GridSearch 最佳)以 95.8% 居首，MLP(32,16,8)和梯度提升树以 93.6% 垫底，MLP(16,8)的标准差 3.9% 最大、逻辑回归的 2.4% 最小。GridSearch 虽然选出参数极不稳定（仅 2/10 次相同），但平均表现优于手动配置的各个 MLP 架构——说明参数调优在小数据上虽噪声大，仍能整体获益。消融实验进一步揭示：决策树在 10% 训练样本时暴跌至 62.2%，SVM（84.4%）最抗数据缩减；标准化对距离模型在本次划分中反而降低准确率，说明预处理效果同样受数据划分支配。这些结果共同说明，单次划分的结论不可靠——多次重复 + 拼接评估 + 消融分析是得出可信结论的必要条件。

**关键词：** 鸢尾花分类；机器学习；重复试验；GridSearch；消融实验

---

## 1. 引言

鸢尾花数据集是 1936 年 Fisher 提出的经典分类数据集，3 个品种各 50 条样本，每条含花萼长宽和花瓣长宽 4 个特征。因特征区分度高、线性可分性强，常被用作机器学习入门实验。

本次大作业要求在 Iris 上对比多种分类方法。实际做下来发现，比出哪个最好不是重点——Iris 太简单，所有方法差距在 2 个百分点内——而是那些反直觉的现象：为什么加深 MLP 反而变差？GridSearch 选的参数为什么极不稳定？为什么不标准化反而更准？这篇报告围绕这些问题展开。

---

## 2. 实验设置

### 2.1 数据集与预处理

数据集 150 条样本，3 类别各 50 条，完全平衡。实验采用 10 次独立重复试验，每次按 70%/30% 随机划分训练集（105 条）和测试集（45 条），最终报告 10 次测试准确率的均值与标准差。所有试验中各模型的预测标签和概率保存至 `experiment_results.json`，训练好的模型、Scaler、训练/测试划分保存至 `session.joblib`。

F1-Score、ROC 曲线和混淆矩阵的评估不再只依赖末次试验的 45 条样本，而是将 10 次试验的全部测试集拼接（共约 450 条）后统一计算。这一拼接策略同样应用于各单方法可视化图（图 4–6、图 12–16）的混淆矩阵，使每张 CM 反映 10 次试验的综合结果而非单次运气。

交叉验证：10 次试验各自在训练集（70%）上做 5 折 StratifiedKFold CV，共 50 个分数，跨试验取均值。参与 CV 的模型为 6 个（逻辑回归、SVM、随机森林、KNN、梯度提升树、朴素贝叶斯），不含 MLP 变体和决策树。

消融实验：基于末次试验的单次划分，末次划分固定 `random_state=42` 以保证可复现。4 类消融结果（特征、数据量、MLP 架构、预处理）同样保存至 session，供 `--analyze` 直接读取。

标准化：对所有距离相关模型（LR、SVM、KNN、MLP）使用 StandardScaler，基于训练集均值与标准差拟合后应用于测试集。树模型和朴素贝叶斯也采用同样预处理以避免不一致，但正常不受量纲影响。

**可复现性**：首次执行 `python classify.py` 完成训练并保存 session 后，任意多次执行 `python classify.py --analyze` 均可从 session 完全复现全部图表（含消融），无需重新训练。

### 2.2 方法概览

共 8 类 12 个模型：

| 类别 | 模型 | 关键参数 |
|---|---|---|
| 线性 | 逻辑回归 | LBFGS, C=1.0 |
| 核方法 | SVM (RBF核) | C=1.0, gamma=scale |
| 树 | 决策树 (d=3) | max_depth=3 |
| Bagging | 随机森林 | 100 棵树, max_depth=5 |
| Boosting | 梯度提升树 | 100 棵, lr=0.1, max_depth=3 |
| 距离 | KNN (k=5) | n_neighbors=5 |
| 神经网络 | MLP(16,8), (32,16,8), (64,32), (32,8) | ReLU, alpha=0.001 |
| 神经网络+调参 | MLP(GridSearch最佳) | 4 架构 ×4 alpha×2 激活=32 组合 |
| 概率 | 高斯朴素贝叶斯 | — |

每个固定参数方法均在 10 次试验中评估。所有 MLP 使用 Adam 优化器、ReLU 默认激活、L2 正则 alpha=0.001、max_iter=1000。GridSearch 在每轮试验中扫描 32 个参数组合（hidden_layer_sizes × alpha × activation），以 5 折 CV 选出该轮最佳参数，然后在测试集上评估。

---

## 3. 实验结果与发现

### 3.1 测试集上的整体表现

各模型在 10 次重复试验中的测试准确率均值与标准差如下（按均值降序）：

| 排名 | 模型 | 准确率 |
|:---:|:---|:---:|
| 1 | MLP (GridSearch最佳) | 95.8% ± 3.4% |
| 2 | SVM (RBF核) | 95.1% ± 2.6% |
| 3 | 决策树 | 95.1% ± 2.8% |
| 4 | 逻辑回归 | 94.9% ± 2.4% |
| 5 | MLP (16,8) | 94.7% ± 3.9% |
| 6 | 高斯朴素贝叶斯 | 94.7% ± 2.8% |
| 7 | 随机森林 | 94.4% ± 3.2% |
| 8 | MLP (64,32) | 94.4% ± 3.2% |
| 9 | KNN (k=5) | 94.2% ± 2.8% |
| 10 | MLP (32,8) | 93.8% ± 3.6% |
| 11 | MLP (32,16,8) | 93.6% ± 3.2% |
| 12 | 梯度提升树 | 93.6% ± 2.7% |

整体区间 93.6%–95.8%，极差仅 2.2 个百分点，所有方法排名在统计显著性上高度重叠。标准差方面：逻辑回归最稳定（2.4%），MLP(16,8)波动最大（3.9%），但没有模型的标准差极端到足以打破排名——各模型的置信区间大部分相互覆盖。

F1-Score 方面，所有模型对 setosa 类的 F1 均为 1.000 或接近（该类与另两类完全线性可分）。差异主要体现在 versicolor 和 virginica 上，MLP(GridSearch最佳)在这两类上 F1 最高（0.938 / 0.942），梯度提升树最低（0.906 / 0.900）。

![综合对比总结](output/12_summary.svg)

图 1 综合对比总结（准确率排名、F1 对比、ROC 曲线）

![混淆矩阵汇总](output/13_confusion_matrices.svg)

图 2 混淆矩阵汇总（8 方法，10 次试验拼接，各约 450 样本）

### 3.2 6 模型交叉验证

交叉验证仅对 6 个传统方法进行（不含 MLP 变体和决策树）。10 次 ×5 折共 50 个 CV 分数，跨试验均值后 CV 准确率与测试集均值处于同一水平（均 93%–96%），但 CV 的标准差（约 3.5%–5.5%）明显高于测试集标准差（2.4%–3.9%），因为每折验证集仅约 21 条样本。

CV 排名与测试排名不完全一致：测试集中随机森林（94.4%）与决策树（95.1%）相比偏低，而 CV 中树模型的 5 折间方差更大，排名波动更多。这在 150 条样本的数据集上属于正常现象——不同评估方式引入的方差足以重新排列。

![交叉验证对比](output/14_cv_comparison.svg)

图 3 交叉验证对比（6 模型，10 次试验 ×5 折）

### 3.3 MLP 变体对比与 GridSearch

5 个 MLP 配置的表现分为三个梯队：GridSearch 最佳（95.8%）、浅层适中宽度（16,8 和 64,32 分别为 94.7% 和 94.4%）、深层或窄网络（32,16,8 为 93.6%，32,8 为 93.8%）。

MLP(16,8) 较 MLP(32,16,8) 高出 1.1 个百分点——加深一层反而下降。原因在于 Iris 仅 150 条样本，三层隐藏层（32→16→8）的 600+ 个可训练参数对训练集形成了过参数化，倾向于记忆噪声而非学习模式。MLP(32,8)同样参数偏多（约 300 个），准确率仅 93.8%。

GridSearch 在测试集上的表现值得注意：**它确实是第一**（95.8%），优于所有手动配置，说明参数调优在整体上能获益。但选出的参数极不稳定：10 次试验中，最常见配置仅出现了 2 次（relu, alpha=0.0001, (16,8) 和 tanh, alpha=0.0001, (64,32) 各 2 次），其余 6 次各不相同。这表明 GridSearch 在小数据上的搜索结果高度依赖验证集划分。虽整体有效，但**不建议依赖其单次输出作为"最优参数"**。

MLP(16,8) 的手动 alpha=0.001，GridSearch 更倾向选 alpha=0.0001（更低的正则化）。在 Iris 这种简单数据上，轻微降低正则化让模型更充分拟合，可能带来约 1 个百分点的提升，但代价是过拟合风险增加。

![MLP 分析](output/05_mlp_analysis.svg)

图 4 MLP 分析（架构对比、损失曲线、GridSearch 结果、alpha 与激活函数扫描）

### 3.4 决策树与集成方法

决策树以 95.1% 排第三，但标准差 2.8% 居中——并非最不稳定。随机森林 94.4%（↓0.7pp vs DT）不升反降，说明在 Iris 上 Bagging 集成未能发挥应有作用。原因在于特征空间极窄（4 个特征，随机子空间仅选 2 个），每次分裂有 50% 概率看不到最强的花瓣特征，被迫使用信息量较低的花萼特征做次优分裂。

梯度提升树 93.6%，垫底，且标准差 2.7% 为所有模型中最低之一。Boosting 对小数据极度敏感：每次迭代试图纠正前一轮的残差，但总共仅 105 个训练样本，每棵树 (max_depth=3) 的拟合目标噪声占比极高，逐轮累加反而可能放大偏差。

![随机森林分析](output/06_random_forest.svg)

图 5 随机森林分析（特征重要性、估计器数量、混淆矩阵、深度扫描）

### 3.5 其他方法

逻辑回归（94.9% ± 2.4%）标准差最小，最稳定。C 值从 10⁻³ 扫到 10² 准确率几乎不变，Iris 对正则化不敏感——线性模型的瓶颈在表达能力本身而非过拟合。

SVM（95.1% ± 2.6%）排名第二。C 值在 0.01–100 区间呈现典型 U 型曲线：太小欠拟合、太大过拟合。gamma 取 0.1 时效果最好。支持向量约占训练集的 15%–25%，RBF 核没有过度记忆样本。

KNN（94.2% ± 2.8%）排名靠后。k=5 默认参数表现稳健，k>15 后准确率开始下降。

朴素贝叶斯（94.7% ± 2.8%）在强相关特征（花瓣长与花瓣宽）上违反独立假设，但 Iris 特征区分度足够高，损失有限。

![SVM 分析](output/03_svm_analysis.svg)

图 6 SVM 分析（C 值、gamma 影响、混淆矩阵、支持向量）

---

## 4. 消融实验

消融实验使用末次试验的单次固定划分（random_state=42），所有模型以 random_state=42 初始化以保证可复现。4 类消融结果已保存在 session 中，`--analyze` 可直接重绘。

### 4.1 特征消融

逐一移除 4 个特征、在 8 个基准模型（含 MLP(32,16,8)）上的测试准确率：

| 移除特征 | 逻辑回归 | SVM | 随机森林 | KNN | MLP(32,16,8) | GBDT | 决策树 | 朴素贝叶斯 |
|---|---|---|---|---|---|:---:|:---:|:---:|
| (全特征) | 0.911 | 0.933 | 0.889 | 0.911 | 0.911 | 0.911 | 0.978 | 0.911 |
| 花萼长 | 0.911 | 0.911 | 0.889 | 0.911 | 0.911 | 0.933 | 0.978 | 0.911 |
| 花萼宽 | 0.911 | 0.911 | 0.889 | 0.933 | 0.933 | 0.933 | 0.978 | 0.911 |
| 花瓣长 | 0.911 | 0.911 | 0.889 | 0.889 | 0.933 | 0.933 | 0.933 | 0.867 |
| 花瓣宽 | 0.933 | 0.933 | 0.911 | 0.844 | 0.933 | 0.889 | 0.911 | 0.889 |

移除花瓣宽影响最大（KNN 跌 0.067, GBDT 跌 0.022）。花萼特征移除后影响很小甚至微升——如 GBDT 去掉花萼后反而上升（0.911→0.933），MLP 去掉花萼后也上升（0.911→0.933）。说明花萼特征在本次划分中近乎噪声。

决策树基线 0.978 为最高，移除任何单特征后仍保持 0.911–0.978，是特征鲁棒性最强的模型。

![特征消融](output/ablation_01_feature.svg)

图 7 特征消融热力图

### 4.2 数据量消融

训练比例从 90%（94 条）逐步减至 10%（10 条，每类仅 3–4 条）：

10% 数据下，决策树从 0.933 暴跌至 0.622（降幅 0.311），为所有模型中最脆弱者。分裂阈值在极少样本下完全不可靠。SVM 和随机森林仍保持 0.844，最为稳健——SVM 的间隔最大化对稀疏样本不敏感，RF 的随机采样降低了过拟合。

朴素贝叶斯在 10% 数据下的 3–4 条/类无法可靠估计高斯分布参数，同样表现较差（0.778）。

![数据量消融](output/ablation_02_data.svg)

图 8 数据量消融曲线

### 4.3 MLP 架构消融

在末次划分上逐一扫描宽度、深度、正则化、激活函数（均以 (16,8)、alpha=0.001、ReLU 为基准变化单一维度）：

**宽度**：4、32 个神经元最高（93.33%），8 个最低（88.89%）。梯度变化非线性，说明小数据下网络容量被随机因素主导。

**深度**：1 到 4 层均在 91.11%–93.33% 之间，无单调趋势。

**正则化**：alpha=0 到 0.1 均为 91.11%，alpha=1 升至 93.33%。强正则化在此次划分中反而有益。

**激活函数**：ReLU（95.56%）优于 Tanh 和 Sigmoid（93.33%）。GridSearch 在 10 次试验中最常倾向于选 Tanh——测试集最优激活函数与 CV 选出的不一致，再次说明 CV 选参在小数据上不可靠。

![MLP 架构消融](output/ablation_03_mlp_architecture.svg)

图 9 MLP 架构消融（宽度、深度、正则化强度、激活函数）

### 4.4 预处理消融

标准化 vs 未标准化在末次划分上的对比：

| 模型 | 标准化 | 未标准化 | Δ |
|---|---|---|---|
| 逻辑回归 | 0.911 | 0.933 | -0.022 |
| SVM | 0.933 | 0.956 | -0.022 |
| 随机森林 | 0.889 | 0.889 | 0.000 |
| KNN | 0.911 | 0.978 | -0.067 |
| MLP (32,16,8) | 0.911 | 0.978 | -0.067 |
| 梯度提升树 | 0.911 | 0.933 | -0.022 |
| 决策树 | 0.978 | 0.978 | 0.000 |
| 朴素贝叶斯 | 0.911 | 0.911 | 0.000 |

距离模型（KNN、MLP、SVM、LR）标准化后反而下降，其中 KNN 和 MLP 降幅最大（-0.067）。花萼特征数值偏大（花萼长 4.3–7.9 cm, 花萼宽 2.0–4.4 cm），未标准化时在欧氏距离中占主导，加之花萼长与花瓣特征存在相关性，间接携带了判别信息。标准化削除了这一量纲放大效应，使得本次划分下准确率下降。这不意味着标准化"有害"——10 次重复中标准化模型在多数划分下总体更优——而是说明单次划分下预处理决策同样可被随机性颠倒。树模型不受影响，朴素贝叶斯的高斯估计受标准化干扰不大。

![预处理消融](output/ablation_04_preprocessing.svg)

图 10 预处理消融对比（标准化 vs 未标准化）

---

## 5. 讨论

各模型准确率极差仅 2.2 个百分点（93.6%–95.8%），所有模型的置信区间高度重叠，统计上无法严格排序。这表明 Iris 的分类任务对方法选择不敏感——任何合理配置的模型都能达到 94%+。真正拉开差距的不是"选什么模型"，而是"怎么评估"。

GridSearch 尽管选出的参数极不稳定（仅 2/10 相同），整体平均准确率却排第一（95.8%）。值得关注的是参数稳定性和最终性能之间的脱节：CV 最优参数不一定推广到测试集最优，但参数搜索过程本身对多个架构和超参数的探索似乎提高了整体性能分布的上限。

集成方法的失效（RF < DT, GBDT 垫底）在小数据集上有明确解释。Iris 仅 4 个特征，随机子空间策略丢失关键特征的概率很高；Boosting 在 105 个训练样本上对残差的逐轮拟合容易放大噪声。单棵树的方差虽大，但如果没有足够的特征多元化，集成带来的收益接近于零。

预处理消融中标准化反而降低准确率的现象，提醒了一个重要事实：在小数据集上，任何单次评估都被划分随机性支配。本次划分碰巧有利于未标准化模型，但 10 次重复的整体均值仍支持标准化的必要性——距离模型的总体均值（有标准化）高于单次消融所见。

---

## 6. 结论

本实验的核心发现：

- **排名不可靠，趋势可信**：12 个模型准确率极差仅 2.2 pp，且标准差重叠，无法严格排序。但定性趋势一致：浅层 MLP（含 GridSearch）和 SVM 处于第一梯队，深层 MLP 和 GBDT 垫底，线性模型和朴素贝叶斯居中。
- **GridSearch 有效但不稳定**：尽管选出参数各次不同，GridSearch 平均准确率最高（95.8%）。不应将 CV 选出的单次参数视为"最优"，但参数搜索过程整体上有益。
- **集成在小特征空间下无力**：随机森林和梯度提升树在 Iris 上未能超越单棵树，甚至更差。特征匮乏时，集成的多样性收益被次优分裂抵消。
- **消融验证了花瓣特征的绝对主导地位**，同时发现花萼特征可能成为干扰项。预处理消融表明标准化效果受数据划分影响——单次结论不足以评判预处理策略。
- **方法论启示**：对 Iris 这种小型数据集，单次 70/30 划分的结论不具备可靠性。多次重复 + 拼接评估 + 消融分析是获得稳健结论的三个必要环节。

---

## 附录：图表索引

| 图号 | 文件名 | 内容描述 |
|:---:|:---:|:----:|
| 图 1 | `output/12_summary.svg` | 综合对比总结（准确率排名/F1/ROC） |
| 图 2 | `output/13_confusion_matrices.svg` | 8 方法混淆矩阵汇总（10 次拼接） |
| 图 3 | `output/14_cv_comparison.svg` | 6 模型交叉验证对比 |
| 图 4 | `output/05_mlp_analysis.svg` | MLP 分析（架构/GridSearch/alpha/激活） |
| 图 5 | `output/06_random_forest.svg` | 随机森林分析（重要性/估计器数/CM/深度） |
| 图 6 | `output/03_svm_analysis.svg` | SVM 分析（C 值/gamma/CM/支持向量） |
| 图 7 | `output/ablation_01_feature.svg` | 特征消融热力图 |
| 图 8 | `output/ablation_02_data.svg` | 数据量消融曲线 |
| 图 9 | `output/ablation_03_mlp_architecture.svg` | MLP 架构消融（宽/深/正则/激活） |
| 图 10 | `output/ablation_04_preprocessing.svg` | 预处理消融（标准化 vs 未标准化） |
| 图 11 | `output/02_decision_boundaries.svg` | 八模型决策边界对比 |
| 图 12 | `output/04_logistic_regression.svg` | 逻辑回归分析 |
| 图 13 | `output/07_gradient_boosting.svg` | 梯度提升树分析 |
| 图 14 | `output/08_decision_tree.svg` | 决策树分析 |
| 图 15 | `output/09_naive_bayes.svg` | 朴素贝叶斯分析 |
| 图 16 | `output/10_knn.svg` | KNN 分析 |
| 图 17 | `output/01_data_exploration.svg` | 数据探索 |
| 图 18 | `output/11_mlp_training_curves.svg` | MLP 训练损失曲线 |

---

## 参考文献

[1] Fisher R A. The use of multiple measurements in taxonomic problems[J]. Annals of Eugenics, 1936, 7(2): 179-188.

[2] 周志华. 机器学习[M]. 北京: 清华大学出版社, 2016.

[3] 李航. 统计学习方法（第2版）[M]. 北京: 清华大学出版社, 2019.

[4] Pedregosa F, Varoquaux G, Gramfort A, et al. Scikit-learn: Machine learning in Python[J]. Journal of Machine Learning Research, 2011, 12: 2825-2830.

[5] Hastie T, Tibshirani R, Friedman J. The Elements of Statistical Learning: Data Mining, Inference, and Prediction[M]. 2nd ed. New York: Springer, 2009.
